name: CI

on: [push, pull_request]

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build binary
      run: ! DOCKER_BUILDKIT=1 docker build .
